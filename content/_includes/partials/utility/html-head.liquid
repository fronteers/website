<!doctype html>
<html lang="{{ locale }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{{ title or site.nl.title }}</title>
    <link rel="stylesheet" href="/assets/css/style.css">
    
    <link rel="preload" as="font" href="/assets/fonts/Fabrik-Bold.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="/assets/fonts/Fabrik-Regular.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="/assets/fonts/Roboto-Bold.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="/assets/fonts/Roboto-BoldItalic.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="/assets/fonts/Roboto-Italic.woff2" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" as="font" href="/assets/fonts/Roboto-Regular.woff2" type="font/woff2" crossorigin="anonymous">
    
    <script src="/assets/js/vendor/focus-visible.5.1.0.min.js" crossorigin="anonymous" async></script>

    <meta name="description" content="{% if locale == 'nl' %}{{ site.nl.metaDescription }}{% else %}{{ site.en.metaDescription }}{% endif %}">
    <meta name="theme-color" content="#">

    <link rel="alternate" hreflang="nl-NL" href="{{ site.nl.baseUrl }}" />
    <link rel="alternate" hreflang="en-GB" href="{{ site.en.baseUrl }}" />

    <meta property="og:site_name" content="{{ site.nl.siteName }}">
    <meta property="og:title" content="{{ title or metadata.title }}" />
    <meta property="og:description" content="{% if locale == 'nl' %}{{ site.nl.metaDescription }}{% else %}{{ site.en.metaDescription }}{% endif %}"/>
    <meta property="og:image" content="">
    <meta property="og:image:alt" content="" />
    <meta property="og:updated_time" content="{{ site.buildTime | date: "%s" }}" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="{{ page.permalink }}">

    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content="{{ title or metadata.title }}" />
    <meta name="twitter:description" content="{% if locale == 'nl' %}{{ site.nl.metaDescription }}{% else %}{{ site.en.metaDescription }}{% endif %}" />
    <meta name="twitter:image" content="" />
    <meta name="twitter:image:alt" content="" />

</head>

{% comment %}
    Include svg shapes so we can refer to them from our CSS.
    Includes the following clip-paths:   
        #curly-brace-down-large

        #curly-brace-open-tiny
        #curly-brace-close-tiny

        #greater-than-open-tiny
        #greater-than-close-tiny

        #parenthesis-open-tiny
        #parenthesis-close-tiny

        #curly-brace-right-small
        #greater-than-right-small
        #parenthesis-right-small

        #parenthesis-down-small
        #greater-than-down-small
        #curly-brace-down-small

        #parenthesis-down-tiny
        #greater-than-down-tiny
        #curly-brace-down-tiny
    {% endcomment %}
{% include svg/shapes.svg %}

{% if locale == 'nl' %}
    {% assign blogposts = collections.published_posts_nl %}
    {% assign activities = collections.published_activities_nl %}
    {% assign jobs = collections.published_jobs_nl %} 
    {% assign members = collections.published_members_nl | random %}
{% else %}
    {% assign blogposts = collections.published_posts_en %}
    {% assign activities = collections.published_activities_en %}
    {% assign jobs = collections.published_jobs_en %}
    {% assign members = collections.published_members_en %}
{% endif %}
