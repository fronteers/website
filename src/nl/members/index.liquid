---
## this file is a liquid file, so we could setup pagination correctly and use liquid in this file. Also check the permalink, which conditionally adds bits to the URL per generated paginated page.
layout: layouts/member-list.liquid
permalink: "/{{ locale }}/{{ page.fileSlug }}/{% if pagination.pageNumber > 0 %}page-{{ pagination.pageNumber }}/{% endif %}index.html"

tags: pages

# navigation
key: members
order: "50"
parent: about

title: Leden

pagination:
  data: collections.published_members
  size: 9
---
